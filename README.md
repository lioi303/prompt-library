# Personal Prompt Skills

三个仅在显式调用时生效的个人 Skill：

- `$question-only`：只回答问题，不改动任何内容。
- `$prompt-polisher`：将 Prompt 变得更容易被大模型理解；只返回可复制的成品 Prompt。
- `$brief-reply`：以最简洁的话回复。

每次修改后提交：`git add . && git commit -m "update prompt skills"`。
