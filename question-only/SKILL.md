---
name: question-only
description: Answers a question without changing files, running mutating commands, sending messages, or taking external action. Use only when explicitly invoked as $question-only.
disable-model-invocation: true
---

# Question Only

这只是个问题，不用改动任何。

- Answer the user's question directly.
- Read-only inspection or research is allowed only when needed for an accurate answer.
- Do not edit files, run state-changing commands, send messages, create external resources, or offer to implement changes.
